# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# OS specific files
.DS_Store
Thumbs.db

# Raft WAL and node data directory
# Contains per-node write-ahead logs (data/node-<id>/wal.log)
# These are runtime artefacts and should not be committed.
data/

# Compiled binary output
raft-kv-store
raft-kv-store.exe
